"""Example signals. `no_overpriced` is the calibration anchor: it MUST grade
near zero / negative under the honest lens (it did −0.004 live over 325 fills).
If it shows a real positive edge here, the fill model is too optimistic."""
